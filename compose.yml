services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: fitmate-app:latest
    container_name: fitmate-app
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:3000"
    environment:
      NODE_ENV: production
